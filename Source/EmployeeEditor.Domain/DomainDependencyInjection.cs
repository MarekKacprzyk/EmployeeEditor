using Autofac;

namespace EmployeeEditor.Domain
{
    public class DomainDependencyInjection : Module
    {
        protected override void Load(ContainerBuilder builder)
        {
            base.Load(builder);
        }
    }
}
